{
  "name": "linear-workflow-system",
  "version": "0.1.0",
  "private": true,
  "description": "Tag a Linear issue with 'jerry' and a fleet of Claude Code agents clones the repo, builds it, runs it in Docker, screenshots it, and opens a PR.",
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run start --workspace services/orchestrator",
    "start": "npm run start --workspace services/orchestrator",
    "build:web": "npm run build --workspace apps/web"
  }
}
